<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header navbar-brand">
      <h3> OneStep </h3>
    </div>
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

    <ul class="nav navbar-nav navbar-right">
      <% if !current_user %>
      <li><%= link_to(" Login", new_user_session_path, method: :get, class: " glyphicon glyphicon-user") %></li>
      <li><%= link_to(" Signup", new_user_registration_path, method: :get, class: " glyphicon glyphicon-log-in") %> </li>

      <% else %>
      <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
       <%= current_user.email %>
       <b class="caret"></b></a>
       <ul class="dropdown-menu">
         <% if current_user.admin? %>
          <li> <%= link_to(" Admin Panel", admin_jobs_path, :class => "	glyphicon glyphicon-user") %></li>
         <% end %>
         <li><%= link_to(" Logout", destroy_user_session_path, :class => "	glyphicon glyphicon-log-out", method: :delete) %></li>
       </ul>
     </li>
     <% end %>
    </ul>
  </div>
  </div>
</nav>
